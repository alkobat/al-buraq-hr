# تقرير التحليل الأمني الشامل
## نظام تقييم أداء الموظفين - شركة البراق للنقل الجوي

**تاريخ التقرير:** 2024-12-11  
**نطاق المراجعة:** جميع ملفات PHP والأمان العام للنظام  
**المراجع:** فريق الأمن السيبراني

---

## 📊 ملخص تنفيذي

تم اكتشاف **13 ثغرة أمنية** متفاوتة الخطورة، منها:
- **4 ثغرات حرجة** (Critical) تتطلب إصلاح فوري
- **3 ثغرات عالية الخطورة** (High)
- **6 ثغرات متوسطة** (Medium)

---

## 🔴 الثغرات الحرجة (Critical)

### 1. SQL Injection في approve.php
**الملف:** `public/approve.php`  
**السطور:** 36-37, 112  
**الخطورة:** حرجة جداً ⛔

**الوصف:**
```php
// سطر 36-37
$strengths = $pdo->query("SELECT description FROM strengths_weaknesses WHERE evaluation_id = {$eval['id']} AND type = 'strength'")->fetchAll();
$weaknesses = $pdo->query("SELECT description FROM strengths_weaknesses WHERE evaluation_id = {$eval['id']} AND type = 'weakness'")->fetchAll();

// سطر 77, 112
$evaluators = $pdo->query("SELECT id FROM users WHERE role = 'evaluator'")->fetchAll(PDO::FETCH_COLUMN);
```

**المخاطر:**
- يمكن للمهاجم حقن أوامر SQL عبر التلاعب بالـ token
- سرقة بيانات المستخدمين والموظفين
- تعديل أو حذف البيانات
- الوصول الكامل لقاعدة البيانات

**الحل:**
استخدام Prepared Statements:
```php
$stmt = $pdo->prepare("SELECT description FROM strengths_weaknesses WHERE evaluation_id = ? AND type = 'strength'");
$stmt->execute([$eval['id']]);
$strengths = $stmt->fetchAll();
```

**الأولوية:** فورية - يجب الإصلاح خلال 24 ساعة

---

### 2. CSRF Protection مفقود في approve.php
**الملف:** `public/approve.php`  
**السطور:** 51-124  
**الخطورة:** حرجة ⛔

**الوصف:**
لا يوجد CSRF token verification عند معالجة POST requests للموافقة/الرفض.

```php
// سطر 51 - لا توجد حماية CSRF
if ($_POST && isset($_POST['action'])) {
    $action = $_POST['action'];
    if ($action === 'approve') {
        // ... معالجة الموافقة بدون CSRF
    }
}
```

**المخاطر:**
- هجمات CSRF تسمح للمهاجم بإجبار المستخدم على الموافقة/الرفض
- تعديل حالة التقييمات بدون علم المستخدم

**الحل:**
```php
session_start();
if (empty($_SESSION['csrf_token'])) {
    $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
}

if ($_POST && isset($_POST['action'])) {
    if (!isset($_POST['csrf_token']) || $_POST['csrf_token'] !== $_SESSION['csrf_token']) {
        die('خطأ أمني: طلب غير صالح');
    }
    // ... معالجة الطلب
}
```

**الأولوية:** فورية

---

### 3. CSRF Protection مفقود في view-evaluation.php
**الملف:** `public/view-evaluation.php`  
**السطور:** 39-76  
**الخطورة:** حرجة ⛔

**الوصف:**
نفس مشكلة approve.php - لا يوجد CSRF protection.

**المخاطر:**
- نفس المخاطر الموجودة في approve.php

**الحل:**
نفس الحل المذكور أعلاه

**الأولوية:** فورية

---

### 4. Authorization Bypass في users.php
**الملف:** `public/admin/users.php`  
**السطور:** 216-223  
**الخطورة:** حرجة ⛔

**الوصف:**
```php
// سطر 216 - الحذف عبر GET بدون CSRF
if (isset($_GET['delete'])) {
    $pdo->prepare("DELETE FROM users WHERE id = ?")->execute([$_GET['delete']]);
    $logger->log('delete', "تم حذف بيانات المستخدم رقم: $id");
    header('Location: users.php?msg=deleted');
    exit;
}
```

**المخاطر:**
- حذف المستخدمين عبر رابط بسيط (GET request)
- يمكن وضع الرابط في صورة أو iframe
- لا يوجد CSRF protection أو تأكيد
- المتغير $id غير معرّف (خطأ برمجي)

**الحل:**
- استخدام POST مع CSRF token
- إضافة confirmation dialog
- إصلاح المتغير $id

**الأولوية:** فورية

---

## 🟠 الثغرات عالية الخطورة (High)

### 5. Missing Authentication في approve.php
**الملف:** `public/approve.php`  
**الخطورة:** عالية 🔶

**الوصف:**
لا يوجد session management - أي شخص لديه token يمكنه الوصول والموافقة/الرفض.

**المخاطر:**
- التوكنات يمكن تخمينها أو سرقتها
- عدم وجود audit trail لمن وافق
- لا يوجد expiration للتوكنات

**الحل:**
- إضافة session-based authentication
- ربط التوكن بـ session ID
- إضافة expiration للروابط (موجود جزئياً - سطر 33)
- تسجيل IP address لمن وافق

**الأولوية:** خلال 48 ساعة

---

### 6. Missing Authentication في view-evaluation.php
**الملف:** `public/view-evaluation.php`  
**الخطورة:** عالية 🔶

**الوصف:**
نفس مشكلة approve.php

**الحل:**
نفس الحل أعلاه

**الأولوية:** خلال 48 ساعة

---

### 7. Password Exposure في Mailer
**الملف:** `public/admin/users.php`  
**السطور:** 316-329  
**الخطورة:** عالية 🔶

**الوصف:**
```php
// سطر 322-327
$plain_password = !empty($_POST['password']) ? $_POST['password'] : $password; 
$mailer->sendEmail($email, $name, 'new_user', [
    'name' => $name,
    'email' => $email,
    'password' => $plain_password // إرسال كلمة المرور بنص صريح
]);
```

**المخاطر:**
- كلمات المرور تُرسل عبر البريد الإلكتروني بدون تشفير
- يمكن اعتراضها أثناء الإرسال
- تُحفظ في سجلات البريد

**الحل:**
- إرسال رابط إعادة تعيين كلمة المرور بدلاً من كلمة المرور
- استخدام One-Time Password (OTP)
- إضافة تنبيه أمني في البريد

**الأولوية:** خلال أسبوع

---

## 🟡 الثغرات متوسطة الخطورة (Medium)

### 8. Weak Password Policy
**الملف:** `public/change_password.php`  
**السطر:** 49  
**الخطورة:** متوسطة 🟡

**الوصف:**
```php
elseif (strlen($new_pass) < 6) {
    $error = "كلمة المرور يجب أن تكون 6 أحرف على الأقل.";
}
```

**المخاطر:**
- كلمات مرور ضعيفة سهلة التخمين
- عدم وجود متطلبات للتعقيد (أحرف كبيرة، أرقام، رموز)

**الحل:**
```php
// الحد الأدنى 8 أحرف
if (strlen($new_pass) < 8) {
    $error = "كلمة المرور يجب أن تكون 8 أحرف على الأقل.";
}
// إضافة متطلبات التعقيد
elseif (!preg_match('/[A-Z]/', $new_pass) || !preg_match('/[a-z]/', $new_pass) || !preg_match('/[0-9]/', $new_pass)) {
    $error = "كلمة المرور يجب أن تحتوي على أحرف كبيرة وصغيرة وأرقام.";
}
```

**الأولوية:** خلال أسبوعين

---

### 9. Default Password "123456"
**الملف:** `public/admin/users.php`  
**السطر:** 126, 302  
**الخطورة:** متوسطة 🟡

**الوصف:**
```php
$password = password_hash('123456', PASSWORD_DEFAULT);
```

**المخاطر:**
- كلمة مرور افتراضية معروفة
- يمكن الوصول للحسابات الجديدة بسهولة

**الحل:**
- توليد كلمة مرور عشوائية قوية
- إجبار تغيير كلمة المرور في أول تسجيل دخول (موجود: force_password_change)

**الأولوية:** متوسطة

---

### 10. Missing Rate Limiting في login.php
**الملف:** `public/login.php`  
**الخطورة:** متوسطة 🟡

**الوصف:**
لا يوجد rate limiting ضد محاولات تسجيل الدخول المتكررة.

**المخاطر:**
- هجمات Brute Force
- اختبار عدة كلمات مرور بدون حد

**الحل:**
```php
// إضافة جدول لتتبع محاولات الفشل
// حظر مؤقت بعد 5 محاولات فاشلة
$stmt = $pdo->prepare("SELECT COUNT(*) FROM login_attempts WHERE ip = ? AND created_at > DATE_SUB(NOW(), INTERVAL 15 MINUTE)");
$stmt->execute([$_SERVER['REMOTE_ADDR']]);
$attempts = $stmt->fetchColumn();

if ($attempts >= 5) {
    $error = "تم حظر تسجيل الدخول مؤقتاً بسبب محاولات فاشلة متعددة.";
    // عرض captcha
}
```

**الأولوية:** خلال أسبوعين

---

### 11. SQL Injection في view-evaluation.php
**الملف:** `public/view-evaluation.php`  
**السطور:** 54, 68  
**الخطورة:** متوسطة 🟡

**الوصف:**
```php
$evaluators = $pdo->query("SELECT id FROM users WHERE role = 'evaluator'")->fetchAll(PDO::FETCH_COLUMN);
```

**الوصف:**
هذا الاستعلام آمن لأنه لا يستخدم متغيرات خارجية، لكن من الأفضل استخدام prepared statements للاتساق.

**الحل:**
```php
$stmt = $pdo->prepare("SELECT id FROM users WHERE role = ?");
$stmt->execute(['evaluator']);
$evaluators = $stmt->fetchAll(PDO::FETCH_COLUMN);
```

**الأولوية:** منخفضة - للتحسين فقط

---

### 12. Missing Input Validation
**الملفات:** عدة ملفات  
**الخطورة:** متوسطة 🟡

**الوصف:**
بعض المدخلات لا يتم التحقق منها بشكل كافٍ (مثل البريد الإلكتروني، الأرقام).

**الحل:**
- استخدام `filter_var($email, FILTER_VALIDATE_EMAIL)`
- التحقق من نوع البيانات بـ `is_numeric()`, `is_int()`
- تنظيف المدخلات بـ `htmlspecialchars()` (موجود في معظم الأماكن)

**الأولوية:** متوسطة

---

### 13. Information Disclosure
**الملفات:** عدة ملفات  
**الخطورة:** متوسطة 🟡

**الوصف:**
رسائل الخطأ في بعض الأماكن تكشف تفاصيل التطبيق:
```php
die('<div style="padding:20px; direction:rtl; font-family:tahoma;">خطأ: ملف <code>vendor/autoload.php</code> غير موجود.<br>يرجى التأكد من رفع مجلد vendor بالكامل.</div>');
```

**الحل:**
- عرض رسائل عامة للمستخدمين
- تسجيل التفاصيل في ملفات logs
- تعطيل `display_errors` في production

**الأولوية:** منخفضة

---

## 🐛 الأخطاء البرمجية

### 14. Undefined Variable $id
**الملف:** `public/admin/users.php`  
**السطر:** 219  
**الخطورة:** عالية 🔶

**الوصف:**
```php
$logger->log('delete', "تم حذف بيانات المستخدم رقم: $id");
```
المتغير `$id` غير معرّف، يجب استخدام `$_GET['delete']`.

**الحل:**
```php
$id = (int)$_GET['delete'];
$pdo->prepare("DELETE FROM users WHERE id = ?")->execute([$id]);
$logger->log('delete', "تم حذف بيانات المستخدم رقم: $id");
```

---

### 15. Code Duplication
**الملفات:** approve.php, view-evaluation.php  
**الخطورة:** منخفضة ⚪

**الوصف:**
الكثير من الكود مكرر بين الملفين (notifications, evaluators query).

**الحل:**
- إنشاء functions مشتركة في ملف helpers
- استخدام classes للعمليات المتكررة

---

### 16. Missing Error Handling
**الملفات:** عدة ملفات  
**الخطورة:** متوسطة 🟡

**الوصف:**
بعض الاستعلامات لا يتم التحقق من نجاحها قبل استخدام النتيجة.

**الحل:**
```php
$stmt = $pdo->prepare("...");
if (!$stmt->execute([...])) {
    error_log("Database error: " . print_r($stmt->errorInfo(), true));
    die("حدث خطأ في معالجة الطلب.");
}
```

---

## 🔐 توصيات الأمان العامة

### 1. إعدادات PHP
```ini
; php.ini recommendations
display_errors = Off
log_errors = On
error_log = /path/to/logs/php_errors.log
expose_php = Off
session.cookie_httponly = 1
session.cookie_secure = 1 (if HTTPS)
session.cookie_samesite = "Strict"
```

### 2. قاعدة البيانات
- استخدام حساب قاعدة بيانات بصلاحيات محدودة (ليس root)
- تشفير الاتصال بقاعدة البيانات (SSL)
- نسخ احتياطية منتظمة ومشفرة

### 3. الخادم
- تفعيل HTTPS (SSL/TLS)
- إخفاء إصدار PHP والخادم
- تفعيل WAF (Web Application Firewall)
- مراقبة السجلات

### 4. الكود
- استخدام dependency management (Composer) محدث
- مراجعة الكود دورياً
- إجراء penetration testing

---

## 📋 خطة الإصلاح حسب الأولوية

### الأولوية الأولى (فوري - 24 ساعة)
1. ✅ إصلاح SQL Injection في approve.php
2. ✅ إضافة CSRF protection لـ approve.php
3. ✅ إصلاح SQL Injection في view-evaluation.php
4. ✅ إضافة CSRF protection لـ view-evaluation.php
5. ✅ إصلاح Authorization Bypass في users.php
6. ✅ إصلاح undefined variable $id

### الأولوية الثانية (48 ساعة)
7. ⏳ تحسين Authentication في approve.php & view-evaluation.php
8. ⏳ إضافة CSRF لـ change_password.php

### الأولوية الثالثة (أسبوع)
9. ⏳ تحسين أمان إرسال كلمات المرور
10. ⏳ تقوية سياسة كلمات المرور
11. ⏳ إضافة Rate Limiting

### الأولوية الرابعة (أسبوعين)
12. ⏳ تحسين validation للمدخلات
13. ⏳ تحسين رسائل الخطأ
14. ⏳ إعادة هيكلة الكود المكرر

---

## 📊 إحصائيات التقرير

- **إجمالي الثغرات:** 16
- **ثغرات حرجة:** 4
- **ثغرات عالية:** 3
- **ثغرات متوسطة:** 6
- **مشاكل برمجية:** 3
- **الملفات المتأثرة:** 8 ملفات رئيسية

---

## ✅ الخلاصة

النظام يحتوي على ثغرات أمنية **حرجة** يجب إصلاحها فوراً قبل الاستخدام في بيئة الإنتاج. أهم الثغرات:

1. **SQL Injection** - يجب إصلاحها فوراً
2. **CSRF Protection** - ضرورية لجميع النماذج
3. **Authentication** - يجب تحسينها للصفحات الحساسة

بعد تطبيق الإصلاحات المقترحة، سيكون النظام أكثر أماناً وجاهزاً للاستخدام.

---

**تم إعداد التقرير بواسطة:** فريق الأمن السيبراني  
**التاريخ:** 11 ديسمبر 2024  
**الإصدار:** 1.0
